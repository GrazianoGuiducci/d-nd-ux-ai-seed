# Current Design Kernel

Version: 0.1.0
Owner: D-ND Design Kernel in `d-nd-ux-ai-seed`.
Source base: `9b91eb9171d9ecb5278f296dba78fd9418b4f024`.

## Present resultant

The design domain has a source-native compositional kernel and four methods:
Perceptual Composition, Source-Grounded Infographic, Source-Grounded Storyboard
and Interaction Quality. Existing seed-adoption skills remain distinct usable
faculties. The kernel composes these by the current work, not a mandatory stack.

One MAIOS profile relates current source identity to the launch problem. It does
not make that brand universal. ChatGPT and coder adapter sources are present;
installation and downstream routing changes have not been performed.

`DESIGN_KERNEL_MANIFEST.json` is the navigable owner map.
`design/EXCHANGE.md` is an optional compact exchange relation.
`design/OWNER_RECONCILIATION.md` records source selection and remaining consumer
work. `design/exercises/2026-09-23/READBACK.md` records the controlled exercise.

## Separate identities and evidence

- Core 0.1.0: source-formed and exercised in the current design work.
- Preview: illustrative artifact, not production site, recorded product demo or
  first-encounter user test.
- Mechanical tests: evidence only for the files and checks reported.
- External receivers: integration/installation unperformed.
- Agentic UX Seed 0.2.1: existing library source/build contract preserved; this
  work does not claim a new full library build or release.
- PR #1 at `2acff293f11cfbb05b306951659d9e92c3e31640`: original candidate remains
  intact. Selected design relations have been re-expressed; the PR is not merged,
  retired or promoted by this work.

## Learning already returned

The controlled formation exposed three corrections incorporated in the methods:

1. A relationship between host surfaces must not visually invent automatic
   synchronisation; source/access and receiver conditions remain intelligible.
2. The current MAIOS chromatic source outranks remembered or early-candidate
   palettes for a current derivative; preserve the older studies as genealogy.
3. New design skills under `skills/` would automatically enter the existing npm
   package. Keep the domain source in `design/` and preserve the public library
   contract until its distribution is explicitly selected.

This records method changes, not stable behavioural assimilation. Later
non-identical use may confirm or alter them.

## Next situated movement

Use the new source to form the selected launch visual/entry artifacts, or perform
the separately selected owner wiring from the reconciliation map. Do not repeat
a whole-system census. Products, Form, site production and the launch timing
remain closed or owned at their existing surfaces. TM3 is not activated here.
