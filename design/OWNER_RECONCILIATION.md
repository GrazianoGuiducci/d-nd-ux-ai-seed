# Source and owner reconciliation

Design source version: 0.1.0 · 23 September 2026.

## Formation and delivery are different

The kernel was formed by exercising source-preserving competence formation,
RepoKernel-native synthesis/transfer and domain knowledge in one current receiver.
The deterministic RepoKernel compiler was not executed: this task needed a living
domain method, not a generated `.repokernel/` tree. Formatting skill files is not
the competence's generative method.

The source body is independently usable. Private source kernels, personal
workspaces, contact records, commercial state and credentials are not included.
External domain roles can be supplied by the receiving environment; a private
repository link is not an indispensable public runtime dependency.

## Qualified source relations

Public baseline: `9b91eb9171d9ecb5278f296dba78fd9418b4f024` in this repository.
Preserved faculties: Agentic UX Seed, D-ND UX-AI Seed, design primitives,
behaviour extraction and promotion contracts. Existing components and release
mechanics were not recreated.

Candidate source: PR #1, `candidate/adaptive-semantic-surface`, exact head
`2acff293f11cfbb05b306951659d9e92c3e31640`. The adaptive perceptual method supplies
meaning/expression separation, independently interpretable visual channels,
negative-space reasoning and artistic references as operations. Its preset
palette, compulsory node limits, mode topology and candidate promotion claims
are not adopted as universal design rules. The old PR remains separately owned.

The authorised shared design field also supplied deeper receiver-side knowledge:
complete-unit selection; responsive semantic parity; relational colour; focus
and motion; interaction-state validity; source-grounded spatial and temporal
explanation. These are re-expressed as portable design methods, not mirrored
receiver files. Receiver-specific checkout paths, hooks and deployment steps do
not become source-domain rules.

The MAIOS profile records the inspected current colour source and distinguishes
it from the old candidate. The controlled example uses that profile as a design
study, not as a new production commitment.

## What now lives here

| Existing function | Current design source | Disposition |
| --- | --- | --- |
| Generic design-element selection | `DESIGN_KERNEL.md` + existing seed skills | Reconciled without another router skill |
| General art direction, medium and brand reasoning | Perceptual Composition | Continuing design-native method formed |
| General responsive/focus/access/recovery method | Interaction Quality | Reconciled portable method; original receiver content preserved |
| Spatial explanation previously local to Launch | Source-Grounded Infographic | Generalised design-native source; old consumer entry not rewritten |
| Temporal/audiovisual explanation previously local to Launch | Source-Grounded Storyboard | Generalised design-native source; channel operation remains external |
| Complete UX component adoption | Existing two seed skills | Preserved, not replaced |
| MAIOS palette and public identity | `design/profiles/maios/BRAND_FIELD.md` | Project profile, not universal method |

This is not a claim that copying pointers has migrated all owners. The selected
source home is ready; existing external entry points remain to be reconciled in
the separately selected integration cycle.

## Exact downstream work by function

**Business Manager:** add one optional design-owner relation for artifact needs
that materially depend on perceptual/interaction form. Keep audience,
relationship, economics and action available there. Do not copy a visual method
or make Business the sole caller.

**Editoriali:** connect the Design Kernel for co-formation of visual artifacts,
copy slots, narration and labels. Keep the writing method and its learning there.
No compulsory Business -> Editoriali -> Design sequence.

**Launch / Public Distribution:** pass platform, wave and encounter purpose only
where useful. Its publication-package relation reaches the new spatial/temporal
methods instead of maintaining duplicate general method bodies. Preserve source
history and launch-specific return points before replacing old entries.

**Codex:** the installed design-element and UX entries should point to this owner
for their shared method while preserving local mechanics and any still-unreturned
learning. Compare relation-by-relation; never overwrite a receiver knowledge base
with a source snapshot. The supplied adapter is not installed by this work.

**ChatGPT / tm7:** a selected router update can point visual/art/interaction work
to this source. The existing UX-AI Kernel Design route remains separate for
in-process explanation and agency. This task does not edit tm7's kernel/router.

**MAIOS site:** keep the public-truth, current page job and production gate at the
site owner. Use this kernel for the selected design. No automatic deployment,
style replacement or adoption of the exploratory preview follows.

**Generic formation owner:** the reusable entry correction is to distinguish a
domain kernel from the package housed in its repository before adding new
competence files to paths included in that package. This is returned here as a
precise integration delta, not written into an external generative kernel.

## Verification scope

The implementation includes source-integrity checks and a separate browser
exercise. Neither is a semantic performance benchmark. Complete library tests,
installed-host exercise and independent first-encounter comprehension remain
separate observations and are not claimed from the local prototype.
