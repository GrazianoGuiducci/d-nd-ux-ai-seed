---
name: dnd-design-kernel
description: Use the owner-native D-ND Design Kernel for visual and interaction composition, art direction, source-grounded spatial or temporal explanation, brand profiles and complete UX-unit adoption. Resolve the actual source before acting; this is a receiver adapter, not another design-method copy.
---

# D-ND Design Kernel — coder adapter

Resolve the checked-out `d-nd-ux-ai-seed` source through the present workspace or
an explicitly supplied owner location. Read `DESIGN_KERNEL.md`, or continue
through the relevant already known faculty. Do not hard-code a PC or VPS path.

For existing UI implementation, preserve the native Agentic UX Seed adoption and
repository-selection contracts. The kernel's `design/` area is not an npm export
or a replacement runtime. Existing library tests and build commands still govern
library changes; new source-only documents do not establish a fresh library build.

Before writing, inspect the actual worktree and incoming state. Preserve foreign
changes; use a clean bounded branch when needed. Install/copy this entry only when
receiver installation is selected. If only this file is copied, the canonical
owner source must still be reachable through a declared local checkout or tool;
a missing owner is not repaired by inventing its methods.

Use the available shell/browser/image tools for the selected artifact and test
actual changed behaviours. Keep generated, rendered, tested, installed and
published states separate. Do not deploy, post or alter accounts through the
existence of the design request.

Return reusable source-independent design learning to its canonical design
faculty, project facts to the project profile and local mechanical adaptations
to this adapter. Do not replace the entire owner method with a receiver snapshot.

Status: source-present, installation not performed by this file.
